{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "health-charted/reference-index",
  "title": "Health Charted Reference Index",
  "description": "Index of reference files with entity coverage and provenance tracking.",
  "type": "object",
  "properties": {
    "_type": {
      "const": "health-charted/reference-index",
      "description": "File type identifier"
    },
    "files": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "file": { "type": "string", "description": "Filename relative to reference/" },
          "topic": { "type": "string" },
          "summary": { "type": "string" },
          "when_to_reference": { "type": "string", "description": "When this file is useful during a session" },
          "entities": {
            "type": "object",
            "description": "Canonical entity names covered by this file, keyed by entity type.",
            "additionalProperties": {
              "type": "array",
              "items": { "type": "string" }
            }
          },
          "derived_from": {
            "type": "object",
            "description": "Provenance: which source files this reference is built from.",
            "properties": {
              "my_data": {
                "type": "array",
                "description": "my-data/ files this reference draws facts from.",
                "items": { "type": "string" }
              },
              "research": {
                "type": "array",
                "description": "research/ files this reference draws evidence from.",
                "items": { "type": "string" }
              }
            }
          },
          "last_interpretation_update": {
            "type": ["string", "null"],
            "description": "ISO date when the interpretation in this reference was last verified against current data."
          }
        }
      }
    }
  }
}
